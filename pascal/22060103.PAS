PROGRAM warna_warni;
USES CRT;
BEGIN
     CLRSCR;
     TEXTBACKGROUND(blue);
     WRITELN('matakuliah praktikun pemrograman terstruktur');
     TEXTBACKGROUND(green);
     WRITELN('program studi D3 teknik informatika');
     TEXTBACKGROUND(red);
     WRITELN('jurusan teknik elekro');
     TEXTBACKGROUND(yellow);
     WRITELN('politeknik negeri pontianak');
     READLN;
END.
